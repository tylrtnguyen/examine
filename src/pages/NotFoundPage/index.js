import React from 'react';
import NotFound from '../../components/NotFound';


const NotFoundPage = () => {
    return (
        <React.Fragment>
            <NotFound />
        </React.Fragment>
    )
};

export default NotFoundPage;